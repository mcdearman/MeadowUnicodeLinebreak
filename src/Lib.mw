-- unicodeLinebreak: where a line of text may be broken, by the Line Breaking
-- Algorithm of Unicode Standard Annex #14.
--
-- A port of Rust's `unicode-linebreak` crate, version 0.1.5, covering Unicode
-- 15.0.0. The tables are generated from the crate by `scripts/generate.sh`,
-- and the tests hold this port to the crate's answers.
--
--   use unicodeLinebreak (linebreaks)
--
--   linebreaks "Hello world!"  -- [(6, Allowed), (12, Mandatory)]

mod Tables
mod Lookup
mod Break
@cfg(test)
mod Cases
@cfg(test)
mod Tests

@pub use unicodeLinebreak.Break (
  BreakClass,
  BreakOpportunity,
  breakProperty,
  linebreaks,
  splitAtSafe
)
@pub use unicodeLinebreak.Break.BreakOpportunity.*

use unicodeLinebreak.Tables as T

-- The version of Unicode the tables follow, as `(major, minor, update)`.
@pub def unicodeVersion = T.unicodeVersion
